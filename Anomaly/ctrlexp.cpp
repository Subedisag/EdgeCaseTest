// this is to test the ctrl expressio with left operator

if (temp = (unsigned(unbase64[src[0]]) << 18)) {}


if (!src[0] || !src[1] || !src[2] ||
          (temp = ((unsigned(unbase64[src[0]]) << 18) |
                   (unsigned(unbase64[src[1]]) << 12) |
                   (unsigned(unbase64[src[2]]) << 6) |
                   (unsigned(unbase64[src[3]])))) & 0x80000000)
  
  
    if (!(ptr = (const char **)alloc->Alloc(
            (my_args.size() + *argc + 1 + args_sep) * sizeof(char *))))
    goto err;
  res = ptr;



if ((data[length - 1 - j] & (1 << k)) != 0)
            break;
